# Test outputs / simulation artifacts (kept locally, not tracked)
Test/

# EnergyPlus build staging dirs (raw build output pasted in for migration into
# sim-EPlus/current/ — not committed; see sim-EPlus/CLAUDE.md)
sim-EPlus/EnergyPlus-PR-*/
